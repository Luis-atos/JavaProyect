pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
            script{
                echo 'Configurando variables -->'
                def workspace = pwd()
                echo "--> ruta ${workspace}"
                def output = bat(script: "mvn -f pom.xml clean ", returnStdout: true)
			    echo output

                }
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
