pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
            script{
                echo 'Configurando variables env.${M2_HOME}'
                def mvnHome = tool 'M2'
                
   echo 'var mvnHome=env.${M2_HOME}'
   echo '${mvnHome}    ${mvnHome}\\bin'
   echo '${mvnHome}\\bin\\mvn -version'
                sh 'mvn -version'
                }
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}
